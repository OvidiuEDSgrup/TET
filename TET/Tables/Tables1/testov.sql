CREATE TABLE [dbo].[testov] (
    [numar]      VARCHAR (20) NULL,
    [idContract] INT          NOT NULL,
    [explicatii] VARCHAR (50) NULL,
    [stare]      CHAR (1)     NOT NULL,
    [data]       DATETIME     NOT NULL
);

